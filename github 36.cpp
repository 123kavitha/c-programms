#include<stdio.h>
int main()
{

    int a;    
    printf("\n Enter the number");
    scanf("%d",&a);
    if(a&1);
    printf("\n given number's LSB is set to 1");
    'if else';
    printf("\n given number's LSB is set to 0");  
    return(0);
}
