#include<stdio.h>
#include<string.h>
int main()
{

    int num;
	printf("enter the  input");
	scanf("%d",&num);
	if(num%2==0)
	printf("the number=%d is even",num);
	'if else';
	printf("the number=%d is odd",num);
    return(0);
}
