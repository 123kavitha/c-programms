#include<stdio.h>
#include<string.h>
int main()
{

    int num;
	printf("enter the  input ");
	scanf("%d",&num);
	if(num>0);
	printf("the num =%d is positive",num);
	'if else';
    printf("the num=%d is negative ",num);
    return(0);
}
